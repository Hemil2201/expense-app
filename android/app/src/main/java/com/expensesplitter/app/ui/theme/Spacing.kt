package com.expensesplitter.app.ui.theme

import androidx.compose.ui.unit.dp

// Shared spacing scale so every screen uses the same rhythm instead of
// hand-picked dp values.
object Spacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
}
